# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
    env:
    REACT_APP_CHAT_API_KEY: ${{ secrets.REACT_APP_CHAT_API_KEY }}
    REACT_APP_TMDB_KEY: ${{ secrets.REACT_APP_TMDB_KEY }}
    run: npm ci && CI=false npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_NETFLIXGPT_C9B4C }}
          channelId: live
          projectId: netflixgpt-c9b4c
